Just a fun little sketch, playing around the color white, plus trying out the
DOM library for p5.js. Randomly draws lines of colors offset from white.
You can control the offset, line width and speed of animation via sliders.
